# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'
use_frameworks!

def shared_pods
    pod 'AFNetworking'
    pod 'SDWebImage'
    pod ‘SVPullToRefresh’
    pod 'JSONModel'
end

target 'VMNews' do
    shared_pods
end

target 'VMNewsTests' do
    shared_pods
end
